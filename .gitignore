vendor
examples

# Notes about a particular application, and the declarations in it the benchmark
# mutations edit. Both describe somebody else's codebase.
benchmarks/FINDINGS.md
benchmarks/cache/targets.json

# Scratch caches and generated output left behind by benchmarks/cache scripts
benchmarks/cache/*/
benchmarks/cache/*.orig
benchmarks/cache/*.diff
benchmarks/cache/*.patch
